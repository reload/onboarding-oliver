#Docker-compose version
version: "3.9"
services:
  web:
    build: docker/images/counter
    ports:
      - "8000:5000"
    volumes:
      - my-fancy-code:/whatever
  redis:
    image: "redis:alpine"
volumes:
  my-fancy-code:
    driver_opts:
      type: none
      device: $PWD/fancy
      o: bind
